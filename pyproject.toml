[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "trabalho-2"
version = "0.1.0"
description = "Implementações de robótica para os trabalhos da disciplina"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["robotics"]